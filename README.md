# Currency app

## 1. How to use

[Nodejs](https://nodejs.org/) is required.

Run in cmd in base directory:
1. `npm i`
1. `npm run build`

This will create dist folder containing the `index.html` which can be ran for testing purposes.
